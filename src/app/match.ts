/**
 * App barrel — embedding match lives in domain.
 */
export {
  l2normalize,
  cosine,
  similarity01,
  matchOne,
  type EnrolledEmbedding,
  type MatchOneResult,
} from '../domain/embedding-match';
