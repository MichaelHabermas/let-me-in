/** @deprecated Prefer `detection-pipeline` — kept for stable import paths. */
export {
  createDetectionPipeline,
  embedFace,
  type AppendAccessLogFn,
  type DetectionPipelineOptions,
} from './detection-pipeline';
