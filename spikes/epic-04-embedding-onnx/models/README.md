# Epic 4 — embedding ONNX

| File | Source URL | Bytes (approx) |
| --- | --- | --- |
| `w600k_mbf.onnx` | https://huggingface.co/deepghs/insightface/resolve/main/buffalo_s/w600k_mbf.onnx | ~13.6 MiB |

InsightFace **MobileFaceNet** backbone trained on MS1MV2 (`w600k` = WebFace600K recognition head naming in buffalo packages). See [FINDINGS.md](../FINDINGS.md) for license notes.

**Note:** `*.onnx` is gitignored by default in this spike folder; re-download with `curl` from the URL above if missing.
