# Admin credentials — copy this file to .env and fill in values.
# Both vars must be set together or neither is used (falls back to dev-default).
# NEVER commit .env to version control.
VITE_ADMIN_USER=admin
VITE_ADMIN_PASS=admin
